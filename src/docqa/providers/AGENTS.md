# Provider Agent Guide

- SDK 객체와 공급자별 오류를 애플리케이션 계층으로 유출하지 않는다.
- 모든 네트워크 호출에 timeout을 두고, 재시도 가능한 오류만 제한적으로 재시도한다.
- 요청당 토큰/문서/비용 상한을 설정할 수 있게 한다.
- 모델 입력과 출력 전문을 기본 로그에 기록하지 않는다.
- 공급자 교체가 가능하도록 내부 계약을 구현하고 contract test를 작성한다.
- 새 공급자의 데이터 보존 및 학습 사용 정책을 ADR과 보안 문서에 기록한다.

