# Application Agent Guide

- 도메인 로직은 FastAPI, Streamlit, 특정 SDK 객체에 의존하지 않게 한다.
- 외부 공급자 의존성은 `providers/` 어댑터를 통해서만 사용한다.
- 공개 함수와 데이터 구조에는 타입을 명시한다.
- 예외에는 민감한 원문을 포함하지 않고, 사용자용 메시지와 내부 오류를 구분한다.
- 새 기능은 최소 하나의 정상 테스트와 하나의 실패/경계 테스트를 추가한다.

