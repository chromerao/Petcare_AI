# API Agent Guide

- API 스키마는 명시적이고 하위 호환성을 고려한다.
- 라우터에서 모델, 벡터 DB, 파일 파서를 직접 호출하지 않는다.
- 입력 길이, 타입, 업로드 제한을 서버에서 검증한다.
- 사용자에게 내부 스택 트레이스, 공급자 응답, 비밀 설정을 반환하지 않는다.
- 새 엔드포인트에는 응답 코드와 실패 동작을 검증하는 API 테스트를 추가한다.

